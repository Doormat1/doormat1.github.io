
---
layout: default
title: Home
---

# Index of Pages

<noscript><p><em>This page is generated by Jekyll. No JavaScript required.</em></p></noscript>

<ul>
  {%- comment -%}
  Exclusions:
  - index and 404 pages
  - files under assets or vendor
  - Jekyll internal directories
  {%- endcomment -%}

  {%- assign excluded_basenames = "index,404" | split: "," -%}

  {%- for page in site.pages -%}
    {%- assign basename = page.path | split: "/" | last | split: "." | first -%}

    {%- if excluded_basenames contains basename -%}
      {%- continue -%}
    {%- endif -%}

    {%- if page.url contains "/assets/" or page.url contains "/vendor/" or page.url contains "/_site/" -%}
      {%- continue -%}
    {%- endif -%}

    {%- if page.dir contains "_layouts" or page.dir contains "_includes" or page.dir contains "_data" -%}
      {%- continue -%}
    {%- endif -%}

    <li>
      {{ page.url | relative_url }}
        {{ page.title | default: page.url }}
      </a>
    </li>
  {%- endfor -%}
</ul>
